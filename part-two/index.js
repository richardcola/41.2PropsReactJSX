/*
DOM, Document Object Model a programming 
interface for web documents, which 
represents the structure of a web page as a 
tree of objects where each object corresponds to a 
part of the page's content, such as elements, 
attributes, and text which provides a way to interact 
with and manipulate the content and structure 
of a web page using scripting languages like JavaScript
*/ 
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);